$(".edit-form").addClass("hidden")
$(".edit_button").removeClass("hidden")
$("#p-uid").text("<%= @profile.uid %>")
$("#p-name").text("<%= @profile.name %>")
$("#p-position").text("<%= @profile.position.name if @profile.position %>")
$("#p-division").text("<%= @profile.division.name if @profile.division %>")
$("#p-skill").text("<%= @profile.skill.name if @profile.skill %>")
$(".dn-add-manager-<%= @profile.id %>").remove();
