<%= form_for request, remote: true,
  html: {id: "dn-form-approve-#{request.id}"} do |f| %>
  <%= f.hidden_field :status, value: "approved" %>
  <%= f.submit t(:approved), class: "btn btn-success" %>
<% end %>
