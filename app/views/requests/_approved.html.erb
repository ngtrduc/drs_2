<%= form_for request, remote: true do |f| %>
  <%= f.hidden_field :status, value: "waiting" %>
  <%= f.submit t(:approved), class: "btn btn-success",
    data: {confirm: t("request.sure_wait", user: request.user.profile.name)} %>
<% end %>
